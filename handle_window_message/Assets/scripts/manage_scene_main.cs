using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class manage_scene_main : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        manage_window_message_main.Instance.ShowWindowMinimize();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
